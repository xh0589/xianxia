# 禁止设计

> 本文件记录用户明确不要的设计。新增任何内容前先对照本表，**不要再做进来了**；已按用户要求移除的，也不要再加回去。

---

## 1. 开局出身 + 天赋（v20.1，已删除）

**来源**：对标《鬼谷八荒》做的开局选择——四选一出身（寒门子弟／世家子弟／散修传人／弃徒之后，给起始灵石铜钱名声气运历练）、五选一天赋（剑骨攻+10%／铁骨防+10%／灵机真元+10%／道心走火-30%／福星气运+15）。

**用户原话**：「你那个出身和天赋做的很简陋，而且我也不是很想用这设计，你删除吧」

**处理**：整体移除。

**删除位置**（如需追溯参考）：
- `js/core/origin-talent.js`（整文件，已删除）
- `仙侠.html` 开局创建页的「出身」「天赋」两个选择卡片区
- `仙侠.html` 角色面板境界下方的「出身: — | 天赋: —」显示行
- `js/app.js` 中 `applyOriginTalentToCharData` 调用、修炼真元/走火入魔倍率、战斗实体攻防倍率、角色状态显示
- `js/core/game-state.js` / `js/app.js` 存档字段 `origin` / `talent`（现已恒为不写入）

**注意**：不要再加任何形式的「开局选出身／选天赋／选背景给永久 buff」设计。要做开局差异，先问用户。

---

## 2. 个人事件栏整栏消失（已修复，禁止回退）

**来源**：角色社交面板里，NPC 的「📜 个人事件」栏曾在玩家不是该 NPC 门下弟子、或人不在该 NPC 所在地、或远程查看时**整栏凭空消失**，不给任何解释——玩家不知道为什么没有了。

**用户原话**：「你个脑残设计的什么鬼，角色社交面板的个人事件栏消失是什么脑残设计，你会不会做游戏」

**处理（v20.3）**：只要该 NPC 有私人线，**整栏恒显示**；未满足的条件逐条写明锁定原因（尚未结识／需为「某派」门下弟子／需亲至「某地」／需要侍妾身份／需要弟子身份／好感≥X（当前Y）／需先完成上一个事件／需先与另一位缔结情缘／需先经历前情／仅女修可经历／仅男修可经历／需先结为道侣／条件未满足）。原因不做短路，全部汇总。

**注意**：不要再做「条件不满足就整栏隐藏」的 UI。锁就亮锁、写清楚为什么锁。

---

## 3. 好感衰减默认开启（已改为默认关闭，禁止回退）

**来源**：与有感情线的对象久不联系（≥3 天）好感会逐渐下降的机制，原先默认开启。

**用户原话**：「掉好感默认不开启，放进设置的“难度设置”里」

**处理（v20.3）**：默认**关闭**。移入设置页「🎮 游戏设置 → 🎚️ 难度设置 → 💞 感情维系衰减」开关（与「⚔️ 战斗难度」同一大框）。开启后：隔 3-6 天每日 -1、隔 ≥7 天每日 -3，好感到 -50 不再下跌，道侣豁免。设置存于既有 `xianxia_settings`（`window._settings.affectionDecay`）。

**注意**：不要再把任何「未互动就惩罚」类机制做成默认开启。

---

## 4. 男主名册占位死字段（已修正，勿再引入）

**来源**：`MALE_LEAD_ROSTER` 冶砚项带 `eventId:'lu_event_rival_placeholder'`、`reconcileId:null` 占位假值，易误导按名册取 ID 的代码。

**处理**：四位名册项统一补齐真实 `eventId`（吃醋）与 `reconcileId`（和好），与女主 `HEROINE_ROSTER` 同构。

**注意**：名册/注册表类结构不要留占位假值；要么填真实 ID，要么不写该字段。
