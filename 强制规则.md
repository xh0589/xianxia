以STRUCTURE.md作为最高工程约束。

每次修改前必须先扫描真实代码，不得仅依据旧STRUCTURE中的“已落地”描述判断功能存在。

每次策划新内容前，需先上网搜索，以便获取素材和灵感

设计宪法（2026-08-26 定稿）：

* 先合乎逻辑，再谈平衡。现存的实现不代表需求——发现与逻辑相悖的旧机制，要主动找出来修改。
* 禁止无叙事依据的人为计数器与隐形配额（如"每日限用3次"）。
* 行为约束必须来自世界本身：时间成本、自身状态（真气/体力）、资源消耗、制度规则。制度须有名有据，把缘由写进文案。
* 对"不合理的反复行为"，用现实式后果链回应，而非弹一句"次数已用完"——例：无故屡闯掌门大殿，初犯守卫拦下劝返；再犯以冒犯尊长论处（轰出、罚扣贡献、禁足）；屡犯加重。惩罚随荒谬程度升级。
* 实体机制只认显式类型标签（physiologyType/elementType 等），禁止从展示名正则反推机制字段；非血肉类型（亡灵/构装体/元素）不吃血肉效果（流血/中毒/疼痛/神魂），免疫时用世界观文案点破。
* 战斗平衡的手段=新增更强的敌人与挑战梯度（精英词缀、具名强敌、深层遭遇构成）；**禁止全局伤害/数值缩放类调整**（用户明令，勿再提议）。

修改前报告：

1. 本次涉及哪些状态及其唯一所有者。
2. 调用了哪些公开API。
3. 修改哪些存档字段。
4. 发出或消费哪些事件。
5. 是否需要旧存档迁移。
6. 将执行哪些自动或人工验收用例。

禁止：

* 新增独立localStorage角色数据。
* 复制一套平行状态。
* 读取DOM作为真实数值。
* 重复声明已有全局函数。
* 使用setTimeout修复依赖关系。
* 只完成页面展示而不接真实逻辑。
* 用“已添加函数”代替完整玩法闭环。

测试纪律（2026-09-01 增补，由试玩复盘得出）：

* **UI 是真理，底层 API 是陷阱**：玩家只通过 UI 交互。任何绕过 UI 直接调 `window.xxx` 底层方法"复现"出来的 BUG，**先怀疑是不是误报**——它极可能是脚本调用方的问题，不是真实玩家 BUG。
* **"耗时 > 10 秒的复现"几乎都是误报**：真实 BUG 触发要快。如果一个"BUG"需要 200 回合才能稳定复现，**先怀疑自己的测试方法**。
* **"看起来对的代码"会让位给用户实证**：当用户报告"我看到了 X 现象"而你的代码分析说"不可能"——**以用户报告为准**。用户能看到的现象永远比你的代码分析更接近真相。
* **数据 ≠ 行为**：看到 `player.skills` 是 object / `player.hp` 不存在，可能是设计意图（v12.7 血量下沉到 22 部位是有意重构），不是 BUG。**不要用"前端工程师的洁癖"评判"游戏设计师的意图"**。
* **报告 BUG 前自问三遍**：
  1. 普通玩家能在前 30 秒内复现吗？（不能 → 脚本风险，不算 BUG）
  2. 这是设计意图吗？（可能 → 先问用户，不要猜）
  3. 真实用户路径是"点哪个按钮 → 看到什么"？（说不出 → 复现路径不成立）
* **必做**：黑盒优先（先纯 UI 交互记录现象）→ 灰盒辅助（看 console 找入口）→ 白盒验证（看源码理解根因）。**顺序不能反**。
* **每个 BUG 报告必须包含**：真实用户路径 + 控制台最小复现命令 + 期望行为 vs 实际行为。

优先顺序（2026-08-26 状态核对标注，防重复开工）：

1. 统一GameState、存档、背包和货币。——✅ v9.10 B1 已落地
2. 建立事件总线与内容ID校验。——✅ v12.1 已落地（EventBus + ContentValidator）
3. 修复新游戏隔离和读档迁移。——✅ 已落地
4. 修复灵兽、合成、任务、秘境、交易闭环。——✅ 基本闭环（灵兽收服v10.0 / 合成v5重写 / 任务追踪v10.0 / 出售重构v11.8 / 秘境探索已有）
5. 补商店和背包的搜索、筛选、排序、数量和比较功能。——✅ 已完成：搜索/筛选/排序/购买数量 v10.0；装备对比弹窗与地图任务标记 v12.4；长行动反馈 v16.5。
6. 再开始人物互动、门派和剧情扩展。——✅ 计划内批次已收官至 v18.7；后续仅按新需求扩展。

每次完成后报告改动文件、API变化、存档迁移、测试结果、人工验收步骤和未解决问题。
