# 引用完整性机械扫描报告（补充）

> 本报告**只收录本轮机械扫描的新发现**，不重复《STRUCTURE核对报告.md》《游戏BUG与设计审查报告.md》已覆盖的内容。
> 扫描方法：grep 全量提取引用 → 逐条验证目标存在性，可复现，不靠判断。
> 扫描对象：`仙侠.html`（脚本与内联事件）、`STRUCTURE.md`（markdown 链接）、跨文件"调用未定义"复核。
> 日期：2026-09-01。

---

## 一、`仙侠.html` 引用——干净（本轮新验证）

| 扫描项 | 结果 |
|---|---|
| `<script src>` 引用 | 141 个，**全部指向真实文件，0 缺失** |
| 内联事件处理器（onclick/oninput/onchange） | 54 个，全部能解析到定义（`ReincarnationIntegration.closeReincarnationModal`/`confirmReincarnation`、`partySystem.showPartyPanel` 等均在；"document.getElementById"/"if"为内联表达式误捕，非真问题） |

**结论**：HTML 侧的文件引用与事件绑定完整性没问题。这是上一轮语义审没覆盖的轴，本轮补扫确认干净。

---

## 二、`STRUCTURE.md` 死链——73 条（本轮新枚举）

### 2.1 真·缺失文档（33 份）

STRUCTURE.md 反复引用下列文档，**实测全部不存在**：

**根级文档（9 份）**：
| 被引文档 | 状态 |
|---|---|
| `GPT属性修改修正.txt` | 缺失 |
| `GPT扩展3.txt` | 缺失 |
| `GPT逻辑审查报告.md` | 缺失 |
| `参考3.txt` | 缺失 |
| `属性现状分析与修改计划.md` | 缺失 |
| `属性系统实施计划.md` | 缺失 |
| `扩展计划.txt` | 缺失 |
| `通用事件.txt` | 缺失 |
| `旧计划/百花谷温蘅感情剧情实施计划.md` | 缺失（`旧计划/` 目录本身不存在） |

**`纯文本/` 目录下文档（24 份）——整个目录上游不存在**：
`纯文本/GPT审查待办实施计划.md`、`纯文本/GPT审核报告2实施计划.md`、`纯文本/GTP审计5.txt`、`纯文本/NPC位置跟随系统实施计划.md`、`纯文本/NPC计划.txt`、`纯文本/P0模块扩充计划.txt`、`纯文本/主流游戏对比与差距分析.md`、`纯文本/剩余任务实施计划.md`、`纯文本/剩余大任务实施方案.md`、`纯文本/城市扩展计划.txt`、`纯文本/基础内容补全开发计划.md`、`纯文本/情境引擎实施计划.md`、`纯文本/战斗死亡系统分析.md`、`纯文本/扩展实施方案.md`、`纯文本/掉落系统优化计划.md`、`纯文本/深度补全计划.md`、`纯文本/物品扩展计划.txt`、`纯文本/社交面板无用选项清理计划.md`、`纯文本/第一批实施步骤.md`、`纯文本/系统连接实施计划.md`、`纯文本/经验系统整合改造计划.md`、`纯文本/设施与门派实施计划.md`、`纯文本/通用事件实施计划.md`、`纯文本/门派入门体系改造计划.md`

### 2.2 关键发现：计划文档目录迁移错位（本轮新发现）

- **上游仓库根目录实际只有 `计划/` 目录（24 份 v18/v19 版本命名的新计划文档，如 `计划/v19.0_宗门职位玩法实施计划.md`、`计划/v19.2_NPC自主人生实施计划.md` 等），根本没有 `纯文本/` 目录**。
- 但 STRUCTURE.md 的计划文档索引（第七节及散落各处的 `纯文本/*.md` 引用）仍指向旧路径。
- **即：项目把计划文档从 `纯文本/` 迁到了 `计划/` 并改了版本命名，但 STRUCTURE.md 的索引整段没跟着更新——指向的全是不存在的旧路径。**
- 本地 `纯文本/` 目录是我本轮建来放三份核对报告的，并非上游原有。

### 2.3 `file:line` 内联引用（~40 条）——双重问题

STRUCTURE.md 大量使用 `[说明](js/xxx.js:行号)` 格式，存在**双重问题**：
1. **markdown 层面是死链**：`file:line` 不是合法的可点链接（文件路径带行号无法解析为文件）。
2. **行号本身大多错的**：这部分已在《STRUCTURE核对报告.md》逐条验证（如 `playerAttackWithMove battle.js:1580` 实为 :2006、`inventory.js:524` 实为 :643、`syncQuestTargetToPlayer party-system.js:747` 实为 :1120 等），此处不重复罗列，仅指出"它们同时也是死链"这一此前未点明的性质。

涉及的文件（行号引用密集区）：`js/app.js`（11 处）、`js/npcs/npc-system.js`（12 处）、`js/inventory.js`（6 处）、`js/party-system.js`（4 处）、`js/battle.js`、`js/beast-taming.js`、`js/equipment.js`、`js/location-system.js`、`js/map/map-markers.js`、`js/npcs/baihua-personal-events.js`、`js/profession-system.js`、`js/quest/quest-system.js` 各 1-2 处。

---

## 三、跨文件"调用未定义"机械复核（结论性，不重复罗列）

本轮对上一轮语义审抓到的"调用了但全仓无定义"类错误做了机械复核，**全部属实**。这些已收录在《游戏BUG与设计审查报告.md》对应系统表内，按"只写新发现"原则此处不再列出，仅记录复核结论：

- `npc.changeTrust`（被调，无定义）✓ 属实
- `window.unlock`（应为 KnowledgeSystem.unlock）✓ 属实
- `window.gameTime.currentDay`（应为 timeSystem.gameTime）✓ 属实
- `Shop.useCredit`/`repayCredit` 引用未声明 `playerDebt`/`playerGold` ✓ 属实
- `addAllItems` 用未声明 `count`/`errors` ✓ 属实
- `inventory.removeItem`（未挂该方法）✓ 属实

---

## 四、明确不在本报告范围（已在前两份报告覆盖）

为遵守"只写之前没提到的"原则，下列内容本轮虽涉及但**已在前两份报告中收录，此处不重复**：
- 游戏内容缺失（主线 006-020 断档、天劫/飞升空壳、31 派无支线、对话树仅 3 NPC、5 装备槽空置、8 突破丹无消费、无教程）——见《游戏BUG与设计审查报告.md》F-1/D-4/D-9/D-13 及经济系统 P1 表。
- 已定义未接入的死代码系统（REALM_UNIQUE_EFFECTS/SKILL_COMBINATIONS/灵根五行/心魔/瓶颈/NPC 生活/双修合击等）——见同报告 D-1 至 D-7 及各系统 P2 表。
- file:line 引用行号偏移的具体清单——见《STRUCTURE核对报告.md》各段。

---

## 总结

本轮机械扫描的**净新增贡献**只有两条：

1. **HTML 侧引用干净**（141 script + 54 handler 全部解析）——这是此前未验证过的轴，确认无问题。
2. **STRUCTURE.md 有 73 条死链**，其中 33 份被引文档真实缺失（9 根级 + 24 份 `纯文本/`），根因是**计划文档已从 `纯文本/` 迁至 `计划/` 并改用 v18/v19 版本命名，但 STRUCTURE.md 索引整段未跟新**——这是文档维护层面的系统性遗漏，此前报告未点明。

修复建议（仅限本报告新发现）：
- 重写 STRUCTURE.md 第七节"计划文档索引"，把 `纯文本/*.md` 全部改指向 `计划/` 下实际存在的 24 份 v18/v19 文档。
- 9 份根级被引文档（GPT属性修改修正.txt 等）若已弃用，从 STRUCTURE.md 删除引用；若仍有价值，补回仓库。
- `file:line` 格式引用统一改为纯文本 `js/xxx.js:行号`（去掉 markdown 链接语法），并按《STRUCTURE核对报告》修正后的行号重排或干脆删除行号。
